
import { CampaignObjective, Platform, Gender } from './types';

export const CAMPAIGN_OBJECTIVES: CampaignObjective[] = Object.values(CampaignObjective);
export const PLATFORMS: Platform[] = Object.values(Platform);
export const GENDERS: Gender[] = Object.values(Gender);
